
## Random notes

### Setup

```
npx @roxi/routify init
npm install --save-dev @rollup/plugin-json
npm install --save-dev @popperjs/core 
npm install --save-dev sveltestrap@v5.0.0-beta.1-3
npm install --save-dev bootstrap@next
npm install --save-dev bootstrap-icons
npm install --save-dev svelte-i18n
npm install --save-dev bytemd
npm install --save-dev @bytemd/plugin-mermaid
npm install --save-dev @bytemd/plugin-highlight
npm install --save-dev github-markdown-css
npm install --save-dev typescript 
npm install --save-dev lodash-es
npm install --save-dev @rollup/plugin-replace
npm install prettier-plugin-svelte prettier
npx prettier --write --svelte-strict-mode --svelte-sort-order options-scripts-markup-styles ./src/**/*.svelte
```

